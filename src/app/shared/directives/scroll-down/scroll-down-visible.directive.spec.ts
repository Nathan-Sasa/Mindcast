import { ScrollDownVisibleDirective } from './scroll-down-visible.directive';

describe('ScrollDownVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDownVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
